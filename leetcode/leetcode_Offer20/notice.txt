本题官方使用的是 确定有限状态自动机
确定有限状态自动机是一类计算模型。它包含一系列状态，这些状态中：
    有一个特殊的状态，被称作【初始状态】
    还有一系列状态被称为【接受状态】，它们组成一个特殊的集合。其中，一个状态可能既是【初始状态】，也是【接受状态】。
起初，这个自动机处于【初始状态】。随后，它顺序地读取字符串中的每一个字符，并根据当前状态和读入的字符，
安照某个事先约定好的【转移规则】，从当前状态转移到下一个状态；当状态转移完成后，它就读取下一个字符。当字符串全部读取完毕后，
如果自动机处于某个【接受状态】，则判定该字符串【被接受】；否则，判定该字符串【被拒绝】。